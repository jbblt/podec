import { type DefaultTheme } from 'styled-components';

const podexTheme: DefaultTheme = {
  // BorderRadius: '5px',
  // colors: {
  // 	main: 'cyan',
  // 	secondary: 'magenta',
  // },
};

export { podexTheme };
